
#include "lista.h"
#include "cadenas.h"

int main(int argc, char* argv[]) {
    //char cadena[]= "  Sofia ////Gallo  ";
    //normalizarCadena2(cadena);
    //puts(cadena);
    //crearLoteDePrueba();
    tLista lista;
    crearLista(&lista);
    /*if(fusionarArchivos(argv[PARAM_STOCK1], argv[PARAM_STOCK2], "Maestro.tmp"))
            leerArchivo("Maestro.tmp");*/
/*    deArchivoOrdenadoALista(&lista, "Maestro.tmp");
    eliminarDuplicados(&lista,comparar);
    while(!listaVacia(&lista))
    {
        recorrerYMostrarLista(&lista,mostrar);
        vaciarLista(&lista);

    }
    return 0;*/
}
